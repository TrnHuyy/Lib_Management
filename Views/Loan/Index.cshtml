@model List<Lib2.Models.Loan>

@{
    ViewBag.Title = "Loan History";
}

<h2>@ViewBag.Title</h2>

@if (Model != null && Model.Any())
{
    <table class="table">
        <thead>
            <tr>
                <th>ID</th>
                <th>BookTitle</th>
                <th>LoanDate</th>
                <th>ScheduleLoanDate</th>
                <th>ReturnDate</th>
                <th>ScheduleReturnDate</th>
                <!-- Thêm các cột khác nếu cần -->
            </tr>
        </thead>
        <tbody>
            @foreach (var loan in Model)
            {
                <tr>
                    <td>@loan.Id</td>
                    <td>@loan.Book.Title</td>
                    <td>@loan.LoanDate</td>
                    <td>@loan.ScheduleLoanDate</td>
                    <td>@loan.ReturnDate</td>
                    <td>@loan.ScheduleReturnDate</td>
                    <!-- Hiển thị các thông tin khác của mượn sách -->
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>No loans found.</p>
}

<div>
    <a asp-action="Index" asp-controller="Book">Tìm sách để mượn</a>
</div> 
