@{
    ViewData["Title"] = "Trang chủ";
}

<div class="text-center">
    <h1 class="display-4">Trang Chủ Của Ứng Dụng</h1>
</div> 

@* <div>
    <a asp-action="Index" asp-controller="Resigter">Đăng Nhập</a>
    <!-- Đường dẫn đến Controller Resigter và Action Index -->
</div>  *@

@model List<Lib2.Models.Book>

<h1>Danh sách sách</h1>

@if (Model.Any())
{
    <table class="table">
        <thead>
            <tr>
                <th>Title</th>
                <th>Author</th>
                <th>Category</th>
                <th>IsBorrowed</th>
                <!-- Thêm các cột khác nếu cần -->
            </tr>
        </thead>
        <tbody>
            @foreach (var book in Model)
            {
                <tr>
                    <td>@book.Title</td>
                    <td>@book.Author</td>
                    <td>@book.Category</td>
                    <td>@book.IsBorrowed</td>
                    <!-- Thêm dữ liệu cho các cột khác nếu cần -->
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>Không có sách nào.</p>
} 
